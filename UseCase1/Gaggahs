Job Execution

You are given in jobs that run in parallel on a processor, Each job i has a required execution time executionTime

processor n operations. In each operations You choose one job as the major job.

The major job runs for x seconds.

Every other unfinished job runs for y seconds, where yex

A job is considered complete once its total mecuted time reaches or exceeds its required execution time and it leaves the pool.

You need to find the minimum number of operations needed for all jobs to complete

1

Example

Suppose n = 5, executionTime [3, 4, 1, 7, 61 x 4, and y-2. Output: 3

Explanation:

1. Choose job 4 as the major job:

Reduce the execution time of job 4 by x and other jobs by y.

executionTime becomes [1, 2, -1, 3, 4]

Job 3 completes.

2. Choose job 4 again: executionTime becomes [-1, 0, -, -1, 21. Jobs 1, 2, and

4 complete.

3. Choose job 5: executionTime becomes [21. Job completes.

Constraints

1<=n<= 105

1s executionTimelil ≤ 109

1<=y<x≤109



def gettinimusoperations(executionTime, x, y)

a arite your code here

fptr open( open(os.environ("OUTPUT PATH"], "w")

executionTime count int(input().strip())

executionTime[]

for in in range(executionTise count):

executionTime item int(input().strip()) executionTime.append(executionTime item)

Snt(input().strip())

yint(input().strip())

result gettinimum Operations(executionTime, x, y)

fptr.write(str(result) + '\n')

fptr.close()
